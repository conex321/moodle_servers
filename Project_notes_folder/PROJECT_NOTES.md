# Project Notes — Moodle Servers (app.canadaemcs.com + source of truth 5.78.128.44)

**Last updated:** 2026-07-16
**Last agent:** Claude
**Session summary (2026-07-16, latest):** **Hetzner fleet management consolidated OUT of this repo** into the new canonical `E:\Claude\Hetzner\` (own git repo). The 5 Hetzner console projects were renamed by Matt (Default→"SchoolConex external modals" 14010860, Hexstruct→"SchoolConex confidential servers" 15178002, "SchoolConex internal moodle" 15188374, Cobionix, + new empty "Personal projects") and 5 fresh per-project API tokens were installed at `E:\Claude\Hetzner\.env` — the `HCLOUD_*` tokens + `HETZNER_CONSOLE_*` creds were **stripped from this repo's `.env`** (kept: `MOODLE_GCP_*`, `FINANCE_PG_*`). Fleet docs (`Hetzner/README.md`, inventory/costing, server notes, ssh_keys registry, cost dashboard) moved out — see `Hetzner/MOVED.md`; only Moodle build/deploy artifacts remain under `Hetzner/`. New fleet-wide SSH: dedicated `hetzner_fleet_ed25519` key deployed to 18/20 servers (`ssh hz-<name>` aliases), 2 gaps documented in `E:\Claude\Hetzner\SSH-GAPS.md`. FleetRefresh task now DAILY 03:00 with per-server cost + ping/SSH health; Master Control dashboard (127.0.0.1:4500) gained a "Fleet detail" section. `atlas-app` (87.99.135.38, discovered by reconciliation) got delete-protection + backups. Fleet run-rate live-verified: **EUR 882.52/mo across 20 servers**. Same day, a parallel session moved this repo root to `E:\Claude\SchoolConex\SchoolConex_Active_servers\Moodle_servers`.
**Session summary (2026-07-08):** Loaded simulated **teacher grading** on elementary.schoolconex.com (`5.78.128.44`) via new reusable tool `Gr1_8/ops/teacher_demo_dashboard/seed_teacher_grading.php` (MAT06/MAT07 graded as `teacherdemo`; demostudent07 left queued). Verified in DB.
**Session summary (2026-07-07):** Rebranded **https://app.canadaemcs.com** to **Canada EMCS** (D-015) via DB/config only; demo creds set + verified externally. Also fixed the `manager` login on elementary.schoolconex.com. Older session summaries: `sessions/accomplishments-pre-split.md`.
**Notes mode:** split
**Total sessions logged:** 12 (pre-split log + this session)

## Current State

- **Hetzner fleet ops live elsewhere since 2026-07-16:** projects/tokens/SSH/cost/health for ALL Hetzner servers → `E:\Claude\Hetzner\` (README there is the fleet index; `_ops\registry\fleet-live.md` is the daily snapshot). This repo keeps only Moodle build/deploy artifacts. Supersedes every fleet-cost/inventory bullet below (they remain as history).

- **app.canadaemcs.com — Canada EMCS-branded (2026-07-07, D-015):** `5.78.190.143` (`emcs-gr1-8-ubuntu-4gb-hil-1`, id 127422698). Moodle 5.1.3 behind host nginx + LE (cert exp 2026-09-17). Stack: `moodle-app` (image `moodle-moodle:latest`, 127.0.0.1:8888) + `moodle-mariadb` + `moodle-phpmyadmin` (127.0.0.1:8080); moodledata 23 GB on local disk (75 GB, 62% used). **Content = twin of elementary.schoolconex.com** (grade 1-8, 57 courses, 22 users). Site now `Canada EMCS`/`EMCS`; EMCS palette + logo + footer applied via DB/config only. Active theme setting is `schoolx` (phantom → renders on Boost). Demo creds delivered: `admin/Schoolx2024!`, `teacherdemo/DemoTeacher2026!` (teaches 10 courses), `demostudent01-10 & demo/DemoStudent2026!`. Rollback: snapshot image 405851690 + `Hetzner/Default-14010860/servers/app.canadaemcs.com/branding-backup-2026-07-07.json`.
- **Fleet cost audit (live API, 2026-07-06 — supersedes ALL older fleet/cost bullets):** **15 running servers across 3 projects** (was 10 on 2026-07-03). Default (`14010860`) = **12 CPX21** (2 grandfathered hil + 10 ash): the 5 prior + Toronto-Academy-EMC, Toronto-Academy-EMC-VietAnh, 3Sixty-Education, and 4 NEW since Jul 3 — `TFS-Highschool` (178.156.152.214, Jul 4), `Futures-Canadian-School` (178.156.159.48, Jul 4, 120 GB vol), `Ashwood-International-Collegiate` (178.156.167.56, Jul 5), `schoolconex-platform` (5.161.208.45, Jul 6, purpose TBD). Hexstruct (`15178002`) = 2 CX23 (`codinginabox`, `schoolconex-finance`). Internal (`15188374`) = 1 CPX31 (`schoolconex-hs-moodle`). **Run-rate $628.79/mo** = servers $492.43 (10×$37.49 ash + 2×$15.53 hil grandfathered + 2×$6.49 cx23 + 1×$73.49 cpx31) + volumes 1,240 GB $95.11 (6 vols @ $0.0767/GB) + backups 4×$7.50 $29.99 + 15 IPv4 $9.00 + 2 snapshots (113 GB) $2.26. Worst case **$672.71/mo** if the 2 hil boxes re-price to list. **Gaps: 10 of 12 Default servers have delete-protection OFF; 11 of 15 have no backups** (only 4 mid-tier schools do — both production boxes, hs.schoolconex.com, codinginabox, finance are all OFF). Dashboard refreshed → `Hetzner/cost-dashboard-2026-07-06.html` (old 2026-07-03 file removed). `Hetzner/README.md` fleet table + run-rate updated to match.
- **Fleet cost audit (live API, 2026-07-03 — superseded by the 2026-07-06 audit above):** 10 running servers, run-rate $444.79/mo (worst case $488.71). Superseded; see the 2026-07-06 bullet for the current 15-server / $628.79 picture.
- **Hetzner fleet inventory (live API, 2026-06-30 — supersedes the "two hosts" framing in older bullets):** Project `14010860` now runs **5 CPX21 servers, all `running`**:
  1. `emcs-gr1-8-ubuntu-4gb-hil-1` (id 127422698, `5.78.190.143`, hil-dc1, created 2026-04-19) → **https://app.canadaemcs.com** — LIVE production (A record + Let's Encrypt confirmed).
  2. `ProductionServerGr-1-8-4gb-hil-3` (id 125507449, `5.78.128.44`, hil-dc1, created 2026-04-01) → IP-only, original snapshot/source server (`Do_not_edit` label). Standby.
  3. `Canada-E-Academy` (id 145396564, `5.161.222.147`, ash-dc1, created 2026-06-26) → **no domain yet**; Moodle live on raw IP, redirects to `/login/index.php`, no SSL. Likely-intended domains registered in Hostinger: `canadaeacademy.com/.ca/.org` or `canadianeacademy.*`.
  4. `Agincourt-International-Academy` (id 146263759, `178.156.152.192`, ash-dc1, created 2026-06-29) → **no domain yet**; Moodle returns HTTP 200 on raw IP, no SSL. No matching `agincourt*` domain found in the Hostinger portfolio.
  5. `Canadian-Virtual-School` (id 146582071, `87.99.158.52`, ash-dc1, created 2026-06-30) → **no domain yet**; Moodle returns "503 under maintenance" (still being set up). Likely-intended: `canadavirtualschool.com/.net/.org` (registered & active in Hostinger).
  Notes: servers 3–5 are functioning Moodle stacks (Apache/2.4.52 Ubuntu) but have **no DNS A-record or HTTPS cert**; the "likely-intended" URLs are inferred from server name + registered Hostinger domains, NOT confirmed by any live DNS record. Cost run-rate is now ~5× CPX21 (up from the ~$31/mo for 2 hosts recorded in older bullets). Re-verify any time: `curl -s -H "Authorization: Bearer $HCLOUD_TOKEN" https://api.hetzner.cloud/v1/servers`. See D-011.
- **High-school Moodle — LIVE (2026-07-01):** New Hetzner project **"SchoolConex - Internal Moodles"** (RW token `HCLOUD_TOKEN_INTERNAL` in `.env`; project created by the user in-console because Hetzner's Heray anti-bot blocks the automated browser). Server `schoolconex-hs-moodle` (**CPX31** 4 vCPU/8 GB/160 GB, `ash`, IPv4 **`87.99.158.41`**, delete-protected) serves **https://hs.schoolconex.com** — 200 OK, Let's Encrypt, **71 Ontario high-school courses** (Moodle 5.1.3). SSH key `hs-moodle-key` = `hetzner_moodle_ed25519` (private at `C:\Users\msefa\.ssh\`). Stack at `/opt/moodle` (`schoolconex-moodle` + `schoolconex-mariadb`, moodle bound to `127.0.0.1:8888` behind host nginx; DB creds root `rootpass` / `moodleuser:moodlepass`; DB snapshot at `/opt/moodle/deploy/`). Source = clone of the local `E:\Course_back_ups\Remote_moodle_setup\moodle-local` Docker stack. See D-014.
- **Production target:** `https://app.canadaemcs.com` (Hetzner VPS `5.78.190.143` = `emcs-ubuntu-4gb-hil-1`, CPX21, us-west; Moodle 5.1.3 in Docker, PHP 8.3.30, nginx, MariaDB 10.11) is **reachable and healthy as of 2026-06-13T18:45Z**. HTTPS returns `200 OK`; SSH root login succeeds with `hetzner_moodle_ed25519`; host uptime 36 days; `moodle-app`, `moodle-phpmyadmin`, `moodle-mariadb (healthy)` all up 5 weeks. The earlier 2026-05-28 unreachability was a client-side network problem from the previous workstation, not a server/host failure (the box never went down). See F-008 (resolved).
- **Source-of-truth instance — now ALSO the LIVE grades 1-8 site (2026-07-04):** `5.78.128.44` (host `ProductionServer-4gb-hil-3`, original snapshot source; `app.canadaemcs.com` was rehomed from it on 2026-04-18–19, see DEPLOYMENT.md). At the user's request it now serves **https://elementary.schoolconex.com** (grades 1-8, 56 courses; parallel to `hs.schoolconex.com` for grade 9-12). What was done, end-to-end: (1) Hostinger DNS A `elementary.schoolconex.com → 5.78.128.44` via the local `hostinger-api-mcp` (`DNS_updateDNSRecordsV1`, `overwrite=false`); (2) the Moodle Docker container (`moodle-app`, image `moodle-app:5.1.3`, Apache 2.4.67/PHP 8.3.31) was moved off host `:80` (`0.0.0.0:80` → **`127.0.0.1:8888`**) in `/opt/moodle/docker-compose.yml`; (3) `/opt/moodle/config.php` rewritten to `wwwroot=https://elementary.schoolconex.com` + `$CFG->sslproxy=true` and **bind-mounted** into the container (`- ./config.php:/var/www/html/config.php`, NOT `:ro` — the image's baked entrypoint does `chown -R www-data /var/www/html` and a read-only mount makes it `set -e` crash-loop; F-note); (4) host **nginx** installed (`/etc/nginx/sites-available/elementary.schoolconex.com`) reverse-proxying `:80/:443 → 127.0.0.1:8888` with `X-Forwarded-Proto https`; (5) **Let's Encrypt** cert via `certbot --nginx` (CN `elementary.schoolconex.com`, exp 2026-10-02, auto-renew) + HTTP→HTTPS redirect; `ufw allow 443`; caches purged. Verified externally: `http` 301→https, `https` login 200 with valid `logintoken`. Config.php + compose backed up to `/opt/moodle/*.bak.20260704-113614`. **Role change:** the box's old `Do_not_edit`/standby framing no longer applies to web/Docker config — treat as production. See CHANGELOG 2026-07-04.
- **Resource / space / cost snapshot (2026-06-14, Hetzner project `14010860`, owner Matthew Rubio):** Both hosts are 2× CPX21 (3 vCPU x86 / 4 GB / 80 GB NVMe, us-west/Hillsboro), idle CPU, ~2 GB RAM free each, no swap. **Disk is the watch item:** prod `/` at **87%** (62/75 GB; `moodledata` 36 GB) and source `/` at **89%** (64/75 GB; `moodledata` 49 GB). **Cost:** latest invoice Jun 3 2026 = **$31.05 (pending/unpaid — has a "Pay now")**, prior May 3 2026 = $21.85 settled; run-rate ≈ **$31/mo** for both servers (May was the first full month with both live; April was partial). **Cost/hardening — all remediated 2026-06-14:** (a) freed disk via `docker builder prune` → prod `/` **87%→79%** (16 GB free), source `/` **89%→84%** (12 GB free); (b) deleted the unassigned primary IPv4 `5.78.144.148` (`primary_ip-127302160`) — was billed for nothing; 4 IPs remain (1 IPv4 + 1 IPv6 per server, all assigned); (c) **enabled delete-protection on both servers** (confirmed via API: `protection.delete=true` on both); (d) 2FA (email OTP) already enabled on the account; (e) created a **read-only** Hetzner Cloud API token `claude-readonly-monitoring`, stored as `HCLOUD_TOKEN` in gitignored `.env` — verified read works, write denied (HTTP 403). Use it for scriptable recurring tracking: `curl -H "Authorization: Bearer $HCLOUD_TOKEN" https://api.hetzner.cloud/v1/servers`. **Still open:** the $31.05 Jun invoice is unpaid (user pays from console).
- **GCP cost/auth comparison snapshot (2026-06-16):** Added `MOODLE_GCP_ACCOUNT_EMAIL`, `MOODLE_GCP_ACCOUNT_PASSWORD`, `MOODLE_GCP_ACTIVE_PROJECT=voltaic-day-495212-i3`, and an auth note to gitignored `.env` at the user's request; do not print the password in notes or reports. Local `gcloud` is installed but active account `matthew@schoolconex.com` has expired OAuth tokens, so `gcloud projects describe`, Compute, SQL, Storage, and Cloud Run inventory all fail with `Reauthentication failed. cannot prompt during non-interactive execution.` Password-only login is not enough for `gcloud`; run `gcloud auth login alex@coursely.ca` interactively. Live verification on Hetzner, not GCP: prod `5.78.190.143` is `80%` full (`57/75G`, `36G` moodledata), source `5.78.128.44` is `84%` full (`61/75G`, `49G` moodledata), and prod DB has **55 courses** plus **2,017 Interactive Video activities**. Cost baseline: existing Hetzner actual run-rate remains about **$31.05/mo** for both legacy CPX21 servers; Hetzner's 2026-06-15 new-order price adjustment lists CPX21 in USA at **$37.49/server/mo** for new orders/rescales; GCP us-central1 estimate is **$36.11/mo per e2-medium VM** (compute $24.46 + 80G balanced PD $8.00 + in-use IPv4 $3.65), **$72.22/mo for two e2-medium VMs**, **$121.14/mo for two e2-standard-2 VMs**, and about **$99.02/mo** for one e2-medium app VM plus a minimal zonal Cloud SQL MySQL instance with 80G storage.
- **Debug state:** `$CFG->debug = 0; $CFG->debugdisplay = 0;` on both the live server and the repo mirror. Re-enabling debug display is now safe with respect to F-001, but `local_edwiserreports/settings.php:197` still emits a PHP 8.3 deprecation noted in the DEPLOYMENT.md §18 known-follow-up — don't re-enable debug display on production until that one is also null-guarded.
- **Plugin state on disk:** 14 plugins deliberately installed: `theme_remui`, `block_edwiseradvancedblock` (patched), `block_edwiser_site_monitor`, `block_edwiserratingreview`, `filter_edwiserpbf`, `filter_edwiserformlink`, `course/format/edwiservideoformat`, `local/edwiserform` (with `edwiserformevents_*` subplugins), `local/edwiserpagebuilder`, `local/edwiserreports`, `local/edwisersiteimporter`, `local/sitesync`, `mod/edwiserform`, `mod/edwiservideoactivity`, `mod/interactivevideo`. No ghost plugins remain (`admin/cli/uninstall_plugins.php --show-missing` is empty).
- **Webservice state:** REST protocol enabled. Service `schoolconex_api` (id=3) is restricted to authorised users; 10 WS functions attached (including all 5 user-required: `core_user_create_users`, `core_user_get_users_by_field`, `enrol_manual_enrol_users`, `core_course_get_courses_by_field`, `core_webservice_get_site_info`). User `schoolconex_api` (id=4) is sole authorised user and has role `ws_api_schoolconex` (id=10) at system context with 16 capabilities (covers all 6 user-required + extras for a realistic integration). Permanent token `bbd004d7f516adf1e41bc7a4a75a8d07` is live with no IP restriction and no expiry — **store in Supabase as `MOODLE_WS_TOKEN`**; IP-restrict and/or set expiry when the backend IP is known.
- **Course idnumber state:** all 55 non-frontpage courses have `mdl_course.idnumber` populated equal to their `shortname` (e.g. `MAT01`, `LAN07`, `SCI08`, `CFR04`, `GEO07`, `HIS08`). Verified via `core_course_get_courses_by_field` lookups on 9 sample codes. This is the key SchoolConex's `courses.code` matches against.
- **Outstanding follow-ups still open:** admin/demo/manager passwords not rotated (DEPLOYMENT.md §Operational note 1), Vimeo domain-locking for `app.canadaemcs.com` not confirmed (§3), `local/edwiserreports/settings.php:197` PHP 8.3 deprecation noted but unpatched (§18 known follow-up), and three missing About-page images remain cosmetic F-006.
- **Local rebuild state:** `moodle_servers/www.appcanadaemcs.com/prepare-build-context.sh` prepares the local Docker build context by hardlinking required plugin zips from `Resources/moodle_plugins/` into ignored `moodleplugins/`. `php.ini`, `patches/choicelist_fixed.php`, and `patches/edwiseradvancedblock_lib.php` are now mirrored from `/opt/moodle/`; every Dockerfile `COPY` source resolves locally. `docker compose --progress=plain build moodle` completed successfully on 2026-05-25 and produced `wwwappcanadaemcscom-moodle:latest`; image inspection confirmed Interactive Video, the Edwiser null guard, the choicelist patch, the RemUI patch, and required PHP extensions.
- **Clone runbook:** `Project_notes_folder/RUNBOOK_MOODLE_CLONE.md` is the canonical single-source-of-truth for bringing up another cloned Moodle instance on a new domain. Encodes every lesson from the 2026-04-18–20 sessions plus the 2026-05 rebuild-patch lessons (VPS provisioning, data transfer, domain re-point, TLS, F-001/F-002 verification, WS token generation, `idnumber` bulk populate, optional `en_local` rebrand, admin-hardening, verification checklist, rollback). Update it every time a new rehome surfaces a gotcha not already documented there.
- **Site-admin users (non-API):** admin `admin` / `Schoolx2024!` (needs rotation); demo `demo` / `DemoStudent2026!` (needs rotation); **manager `manager` / `Admin123!` (user id=5, email `admin@canadaemcs.com`)** — assigned the custom `courseenroller` role (id=11) at system context. Can create users + enrol into any course (via manual enrol, with permission to assign student/teacher/editingteacher); cannot create/back-up courses, cannot access site:config. UX caveat: because user creation in Moodle lives under Site administration → Users, the manager sees a **filtered** Site administration link that exposes only `Users → Accounts` (Add a new user, Browse list of users, Bulk user actions). Every other admin subsection is hidden. This is the tightest "no admin panel" achievable while still allowing in-UI user creation. See D-007.
- **Brand rename state:** 561 user-facing `$string[...] = '...Moodle...'` values replaced with `EMCS` across 119 component files via the `en_local` mechanism — overrides live at `/var/moodledata/lang/en_local/*.php`. Every page title, breadcrumb tail, help text, config description, and footer link that previously said "Moodle" now says "EMCS". Remaining "Moodle" tokens in rendered HTML are limited to non-user-visible CSS class names (`.moodle-has-zindex`, `.moodle-actionmenu`), JS config paths (`M.cfg.moodle...`), and internal URLs (`/mod/`, `/course/`, `/lib/requirejs.php/...`) — intentional; changing them would break the application. Reversible by `rm /var/moodledata/lang/en_local/*.php` + `admin/cli/purge_caches.php`. See D-008.
- **CodingInABox host (NEW — separate from the Moodle fleet; IN PROGRESS, 2026-06-30):** Standing up `https://www.codinginabox.com` (SchoolConex). Key finding: codinginabox is **NOT Moodle** — source at `E:\Claude\SchoolConex\www.codinginabox.com\Website` is a bespoke static site + custom JS SPA LMS with Supabase (auth/DB), Stripe (subscriptions), Node `arduino-compile`/`lab-verify` microservices (Docker), and a Vercel deploy config; lesson videos are external (Vimeo). Decision: host the real platform on one cheap Hetzner box (nginx static + arduino-compile container with `/compile` reverse-proxied same-origin), keeping Supabase/Stripe/Vimeo in the cloud. Target server: **cx23** (2 vCPU x86 / 4 GB / 40 GB, ~€6.49/mo) in `fsn1` — EU chosen over US `ash` which is ~3.4× the price (CPX11 €5.99 EU vs €20.49 US; CPX11 now unorderable EU, cx23 is the current-gen replacement). Staged: hcloud CLI installed; key `Resources/ssh_keys/hetzner_codinginabox_ed25519` generated + uploaded to both projects; `servers/www.codinginabox.com/nginx_codinginabox.conf` written; Dockerfile already shipped in the app repo (`services/arduino-compile/Dockerfile`, x86, AVR core baked in). **LIVE + STAGED (2026-06-30):** the account-wide limit (was 5/5 across all projects; a new project did NOT bypass it) was raised via a console request (Servers→10), and the server is provisioned: **`codinginabox`** id `146601361`, **`167.233.141.24`** (IPv6 `2a01:4f8:c010:9216::1`), cx23/fsn1, Ubuntu 24.04, delete+rebuild protection on, in project Hextract. Site deployed to `/var/www/codinginabox`; arduino-compile Docker container runs on `127.0.0.1:8088` (restart=unless-stopped); nginx serves static + reverse-proxies `/compile`+`/health`. **Staged at `https://box.codinginabox.com`** (additive Hostinger A record + Let's Encrypt cert, expires 2026-09-28); verified end-to-end over HTTPS (site 200, `/health` ok, `/compile` returns real Intel HEX, lab page `LAB_COMPILE_URL=location.origin` works same-origin). **`www`/apex still point to Vercel — NOT cut over** (Matthew chose staging-first). Follow-up email to Hetzner drafted (not sent) confirming the limit + Primary IPs. See D-012.
- **Repo reorganization (2026-06-30, see D-013):** The working tree was restructured **provider → Hetzner Cloud project → server (folder named after its linked domain) + costing**. New top-level `Hetzner/` (parent for all cloud infra) and `Hostinger/` (registrar data). `Hetzner/Default-14010860/` holds the 5 Moodle servers; `Hetzner/Hextract-15178002/` holds CodingInABox. SSH keys moved to `Hetzner/ssh_keys/`. A live API refresh during the reorg confirmed **6 running servers** and produced `Hetzner/README.md` + per-project `inventory.md`/`costing.md` + `Hetzner/ssh_keys/KEYS.md`. **Current run-rate ≈ $150/mo** (2 grandfathered hil CPX21 ≈ $15.53 ea, 3 ash CPX21 @ $37.49, 1 fsn1 cx23 @ $6.49). Confirmed domain labels: `canadaeacademy.com` and `canadavirtualschool.com` are registered in Hostinger; **Agincourt has no registered domain** (folder uses the server name pending confirmation).


## Open Questions / Next Steps

1. ~~**URGENT: Restore production infrastructure access for F-008.**~~ **RESOLVED 2026-06-13** — both hosts reachable, SSH login verified on each, all containers healthy; the 2026-05-28 unreachability was client-side networking, not a host outage. Hetzner console credentials stored in gitignored `.env`. See F-008.
2. ~~**Complete the web services + token setup**~~ **RESOLVED 2026-04-19** — service `schoolconex_api` (id=3) is configured with 10 functions + restricted-users + authorised user `schoolconex_api` + permanent token `bbd004d7f516adf1e41bc7a4a75a8d07`. Course `idnumber` populated on all 55 courses to match `shortname`. See Current State and D-006. **Remaining follow-up tasks:** (a) IP-restrict the token once the Supabase backend IP is known, (b) store the token as `MOODLE_WS_TOKEN` in Supabase secrets, (c) run SchoolConex's audit endpoint with `dry_run: true` to confirm no `courses.code` values mismatch Moodle `idnumber`; fix any individual mismatches via admin UI `Course → Settings → Course ID number`.
3. **Patch F-003** (`local/edwiserreports/settings.php:197` null-to-stripos deprecation) before anyone re-enables debug display. Pattern from F-001/D-002: capture patch file → add `COPY` line to Dockerfile → `docker cp` into running container. Low urgency but necessary for log hygiene + debug-safety.
4. **Rotate admin, demo, and manager passwords on the live server** (DEPLOYMENT.md follow-up §Operational note 1). The snapshot carried `admin / Schoolx2024!`, `demo / DemoStudent2026!`, and `manager / Admin123!` — all still need rotation. **Note (2026-07-07):** the `manager` account did NOT exist on the elementary.schoolconex.com box (`5.78.128.44`) — it was never carried over from the snapshot. It was (re)created there as user id=22 with the site Manager role and password `Admin123!` after the user reported the login failing. That weak, shared password on a near-admin account on a public host makes rotating *this* account the highest-priority item here. **Also (2026-07-07, D-015):** `app.canadaemcs.com` now has intentional demo creds delivered to the user — `admin/Schoolx2024!`, `teacherdemo/DemoTeacher2026!`, `demostudent01-10 & demo/DemoStudent2026!`. These are demo props, but `admin/Schoolx2024!` is still the shared weak admin password and should be rotated before any real use.
5. ~~**Rebrand app.canadaemcs.com to Canada EMCS**~~ **DONE 2026-07-07 (D-015)** — site name, EMCS palette (Inter/Outfit, `#1B4332`/`#D4AF37`), scrubber, Canada EMCS footer, and EMCS logo/favicon applied via DB/config only (no re-clone). Verified externally via Playwright. Rollback = snapshot image 405851690 + `branding-backup-2026-07-07.json`. **Discovery logged:** the box had been silently redeployed into a twin of elementary.schoolconex.com (grade 1-8, `moodle-moodle:latest` image, phantom `schoolx` theme setting) — this earlier redeploy is still undocumented in this repo and predates today's session.
5. **Confirm Vimeo domain locking** allows `app.canadaemcs.com` (DEPLOYMENT.md follow-up §Operational note 3). Interactive Video modules will 403 for students otherwise.
6. **Resolve F-006 About-page image 404s** by uploading the three missing `section_*.png` images or removing their references from `mdl_edw_pages.id=5.pagecontent`.
7. **Monitor for additional PHP 8.3 deprecations** in the installed plugin set. With 11 new plugins installed as of 2026-04-19, the deprecation surface area roughly tripled. Enable debug log (not debug display) in a staging context if possible, and grep Apache logs for `Deprecated:` over a 24h window to catch anything else.
8. **Decide whether to notify the Edwiser plugin upstream** about the `file_exists(null)` bug (F-001), the `stripos(null, ...)` bug (F-003), and the D-009 jQuery/AMD fixes, so local patches can be retired in a future vendor release.
9. **Scheduled production image rebuild** — local image build is verified as of 2026-05-25. When a production maintenance window is available, run the same build on the server and `docker compose up -d moodle`; verify F-001/F-002/D-009 after the rebuilt container starts.
10. **Reauthenticate GCP before any "actual GCP bill" comparison.** Repo-root `.env` now contains the Moodle GCP account context, but local `gcloud` cannot use a password non-interactively. Run `gcloud auth login alex@coursely.ca` in a browser, then re-run Compute/Billing inventory for project `voltaic-day-495212-i3` or the actual Moodle project if different.
11. **CodingInABox go-live — server now LIVE; finish DNS cutover.** Limit increase cleared and the `codinginabox` cx23/fsn1 box is provisioned (`167.233.141.24`, project Hextract); site + arduino-compile container deployed, staged at `https://box.codinginabox.com`. Remaining: cut `www`/apex `codinginabox.com` over from Vercel when Matthew approves. The Supabase/Stripe/Vimeo steps in `E:\Claude\SchoolConex\www.codinginabox.com\Website\GO-LIVE-RUNBOOK.md` require Matthew's third-party logins. See `Hetzner/Hextract-15178002/`.
12. **DNS + SSL for the new Moodle schools, and confirm Agincourt's domain (reorg follow-up, see D-013).** `Canada-E-Academy` (`5.161.222.147`) → map `canadaeacademy.com`; `Canadian-Virtual-School` (`87.99.158.52`) → map `canadavirtualschool.com` (both registered in Hostinger), then issue Let's Encrypt per RUNBOOK phases 4–9. **`Agincourt-International-Academy` (`178.156.152.192`) has NO registered domain** — confirm/register one, then rename `Hetzner/Default-14010860/servers/agincourt-international-academy/` to match.
13. **Refresh the Hetzner repo docs + fix cost hygiene (from the 2026-07-03 audit).** `Hetzner/README.md` and the per-project `costing.md` files are missing the 3 new Default servers, all extras (volumes/backups/IPv4/snapshots), and carry a stale $193.94 worst-case figure (true: $488.71). Decide and apply a deliberate backup + delete-protection policy: production hosts currently have NO Hetzner backups while 4 mid-tier school servers do, and the 6 post-Jun-26 Ashburn servers are not delete-protected. Also evaluate the ~$159/mo US-vs-EU premium on the 6 ash CPX21s, confirm the 2 old hil snapshots (113 GB, $2.25/mo) are still needed, and document Hostinger domain renewal costs somewhere.

14. **(2026-07-16 update to #13)** Fleet docs/cost hygiene is now automated: FleetRefresh (daily 03:00) regenerates `E:\Claude\Hetzner\projects\<slug>\{inventory,costing}.md` from the live API; backup + delete-protection policy is enforced fleet-wide (20/20 covered). Still open from #13: the ~EUR 318/mo US-vs-EU premium on the 12 ash/hil cpx21s, and `schoolconex-platform` + `atlas-app` purpose confirmation.

## Index
- Decisions → `decisions.md`
- File map → `file-map.md`
- Failures & resolutions → `failures.md`
- Conventions & gotchas → `context.md`
- Session history → `sessions/INDEX.md`
- Raw change log → `CHANGELOG.md`

## How to read this folder
1. Read this file end-to-end.
2. Read `context.md` before touching code.
3. Check `sessions/INDEX.md` for the last 3 sessions.
4. Load other files on demand.
